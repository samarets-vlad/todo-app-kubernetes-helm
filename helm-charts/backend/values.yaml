replicaCount: 2

image:
  repository: python
  tag: "3.9.21-alpine3.21"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 5000
  targetPort: 5000

mysql:
  host: "mysql"
  database: "tododb"
  user: "todouser"
  password: "todopassword"

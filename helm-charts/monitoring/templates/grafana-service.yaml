apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "monitoring.labels" . | nindent 4 }}
spec:
  selector:
    app: grafana
  ports:
  - port: 3000
    targetPort: 3000
  type: {{ .Values.grafana.service.type }}
